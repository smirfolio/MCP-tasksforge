[project]
name = "mcp-tasksforge"
version = "0.1.0"
description = "The Tasksforge.ai MCP server to fetch projects, for software engineering vibe coding workflow"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.12.3",
]
